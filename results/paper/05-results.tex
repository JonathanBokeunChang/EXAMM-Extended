 % ---------------------------------------------------------------------------
% Results section (\input as 05-results from sigconf.tex).
%
% STATUS (2026-08-06): eight result tables. Prose is written for
% tab:pooling-ablation and sec:examm-vs-transformers; the remaining tables are
% still carried by their comment blocks rather than body text. In table order:
%   tab:pooling-ablation  -- does pooling (Table~\ref{tab:constructions}) beat
%                            individual training? Read this FIRST -- it
%                            justifies the pooled construction every other
%                            table in this section then assumes. PROSE WRITTEN.
%   tab:results-2022-2023 -- the main model comparison (EXAMM/DLinear/
%                            transformers), using the strategy defined right
%                            below. DLinear alone still dashed.
%   tab:sharpe            -- risk-adjusted view of the same cells. Kept as a
%                            SEPARATE table: packing "return (Sharpe)" into
%                            each book-size cell was tried and reverted --
%                            two different-unit numbers per cell inside a
%                            \resizebox'd 10-column table shrinks the type to
%                            the point of illegibility.
%   tab:transformer-hparams -- every transformer hyperparameter with its source
%                            script and pinned commit. Machine-readable copy,
%                            including resolved argv, in
%                            results/transformer_hyperparameters.md.
%   tab:hp-grid           -- EXAMM island x repopulation grid, trading returns.
%   tab:hp-grid-sharpe    -- same grid, Sharpe.
%   tab:net-of-cost       -- same cells, after transaction costs.
%   tab:midhighmid        -- replication on four independent 50-stock draws.
%   tab:ina219_results    -- on-device (Raspberry Pi Zero) deployment cost,
%                            connects back to the parameter-count columns in
%                            tab:results-2022-2023.
%

%
% REMOVED 2026-08-02 (recoverable from git c9134cc9):
%   - tab:grid-robustness -- the 225-cell long/short sweep.
%   - The Reversal control row, from every table in this section.
%   - The "gate threshold is not in comparable units" subsection
%     (sec:gate-units) and tab:gate-units, which explained Crossformer's
%     two 0.00 cells via a per-model percentile threshold sweep. The
%     mechanism is now summarised in body prose under "EXAMM vs Transformers"
%     and in the table footnote instead.
%
% The \clearpage at the end of 04-datasets.tex already flushes anything
% queued from that section before this one's floats begin, so nothing
% drifts across the \input boundary. Do NOT add a second \clearpage here --
% two adjacent \clearpage calls force an extra, needless page break (this
% is what was leaving page 2 half-empty: the first one truncated section 4
% early, the second then stranded this section's heading alone on its own
% page before its tables could join it).
% ---------------------------------------------------------------------------

\section{Results}
\label{sec:results}

\subsection{Pooled vs.\ Alternative Constructions}
% ---------------------------------------------------------------------------
% POOLING ABLATION -- goes FIRST in this section, ahead of the STRATEGY block
% below, because every other table in this section takes the pooled
% construction as given; this is the one table that actually justifies it
% rather than asserting it. L/S~10 here uses the identical conditional
% long/short strategy defined in the STRATEGY comment immediately below --
% not redefined here to avoid saying it twice.
%
% Does the pooled-panel construction (Table~\ref{tab:constructions}) actually
% outperform the individual (one-model-per-stock) alternative it replaces?
% Individual here is EXAMM trained one stock at a time (50 independent models x 10
% runs each, scripts/stock_run/anvil_cohort_individual.sb), same cohorts/windows as
% the pooled comparison below, predictions averaged across the 10 runs per stock
% (ensemble, matching how every other model in this paper is scored) before scoring
% IC/MSE/MAE and trading. Verified: expected_RET identical across all 10 runs per
% stock before averaging (rules out a file-pairing error); one run (WRB, cohort 2021)
% failed genome verification and was excluded, so that stock's ensemble is 9 runs.
%
% READING: MSE/MAE are nearly identical between individual and pooled in both years --
% pooling does not make each stock's point prediction more accurate. IC is where the
% gap actually lives (individual ~0 or negative both years; pooled clearly positive
% both years), which is the direct mechanism for the trading gap: a long/short
% strategy needs predictions that rank stocks against each other, andsonly the pooled
% model reliably produces that. State this explicitly in body prose -- it's the
% mechanistic explanation the rest of the section's numbers otherwise leave implicit.
% ---------------------------------------------------------------------------


% \begin{table*}[t]
% \centering
% \caption{Pooling vs. individual (one model per stock) training, both 10-run
% ensembles, identical cohorts/windows/strategy as
% Table~\ref{tab:all-datasets}.}
% \label{tab:pooling-ablation}
% \begin{tabular}{llrrrr}
% \toprule
% Trade year & Construction & IC & MSE & MAE & L/S~10 \\
% \midrule
% 2022 & Individual & $+0.0001$ & $4.726{\times}10^{-4}$ & $0.01584$ & $+13.36$ \\
% 2022 & \textbf{Pooled (ours)} & $\mathbf{+0.0030}$ & $\mathbf{4.630{\times}10^{-4}}$ & $\mathbf{0.01566}$ & $\mathbf{+14.40}$ \\
% \midrule
% 2023 & Individual & $-0.0050$ & $3.037{\times}10^{-4}$ & $0.01232$ & $-1.08$ \\
% 2023 & \textbf{Pooled (ours)} & $\mathbf{+0.0195}$ & $\mathbf{2.930{\times}10^{-4}}$ & $\mathbf{0.01205}$ & $\mathbf{+31.99}$ \\
% \bottomrule
% \end{tabular}
% \end{table*}

Prior work employed two constructions~\cite{lyu2025evostar}. The \emph{individual} construction fits a separate model per stock, so no information is shared across the universe. The \emph{concatenated} construction fits one model over all stocks by stacking their predictors into a wide series (300 inputs, 50 outputs), making input dimensionality scale with universe size. The \emph{pooled panel} adopted here retains the six-predictor, single-output structure of the individual construction while fitting one model across all stocks, so parameters are shared across the universe and input dimensionality remains fixed regardless of how many stocks are included.

Table~\ref{tab:pooling-ablation} compares pooled and individual training on the Baseline Portfolio, reporting test-period IC, MSE, and the return of a simple long/short portfolio that holds the ten highest-ranked and shorts the ten lowest-ranked stocks. Pooling improves every metric in both the 2022 and 2023 folds. The gain in MSE is modest, however the effect on cross-sectional ranking ability is substantially larger: individual training produces near-zero IC in 2022 and negative IC in 2023, meaning its predictions carry little or no usable signal about relative performance, whereas pooled training attains positive IC in both folds. Because the long/short strategy allocates on relative rather than absolute predictions, this difference in ranking quality, rather than the small MSE gap, drives the trading results. Pooling improves the long/short return in 2022 and, in 2023, converts a strategy that loses money under individual training into a profitable one. We therefore adopt pooled training for all subsequent experiments.


\begin{table}[t]
\centering
\caption{Pooling ablation on the Baseline Portfolio.}
\label{tab:pooling-ablation}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{l rrr rrr}
\toprule
& \multicolumn{3}{c}{Test 2022} & \multicolumn{3}{c}{Test 2023} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7}
Construction & IC & MSE & L/S~10 & IC & MSE & L/S~10 \\
\midrule
Individual & $+0.0001$ & $4.726\text{e-}4$ & $+13.36$ & $-0.0050$ & $3.037\text{e-}4$ & $-1.08$ \\
Concatenated &  &  &  &  &  &  \\
\textbf{Pooled} & $\mathbf{+0.0030}$ & $\mathbf{4.630\text{e-}4}$ & $\mathbf{+14.40}$ & $\mathbf{+0.0195}$ & $\mathbf{2.930\text{e-}4}$ & $\mathbf{+31.99}$ \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{EXAMM Hyperparameter Search}
\label{sec:examm-hparams}

Speciation is central to convergence behaviour in neuroevolutionary search: the island
population structure determines how much architectural diversity is maintained, while the
repopulation interval controls how aggressively converged islands are reseeded from elsewhere in
the population. Too frequent repopulation discards partially converged architectures before they
can be refined; too infrequent repopulation allows islands to stagnate. We therefore swept both
parameters jointly, over island counts $\{10, 20, 40\}$ and repopulation intervals
$\{250, 500, 1000\}$, evaluating all $9$ configurations on both test cohorts.

Table~\ref{tab:hp-grid} reports the results. The repopulation interval is the more consequential
of the two parameters: the $500$ setting yields the strongest returns at every island count in
both cohorts, whereas $250$ and $1000$ are inconsistent and in several configurations produce
losses. Island settings differ across 2022 and 2023 test year.
Given that additional islands increase search cost without a corresponding gain, we adopt
$(10~\text{islands}, 500)$ repopulation frequency for all subsequent experiments.



\begin{table}[t]
\centering
\caption{EXAMM hyperparameter search}
\label{tab:hp-grid}
\begin{tabular}{l rrr c rrr}
\toprule
& \multicolumn{7}{c}{Repopulation interval} \\
\cmidrule(lr){2-8}
& \multicolumn{3}{c}{Test 2022} & & \multicolumn{3}{c}{Test 2023} \\
\cmidrule(lr){2-4} \cmidrule(lr){6-8}
Islands & $250$ & $500$ & $1000$ & & $250$ & $500$ & $1000$ \\
\midrule
$10$ & $-2.50$ & $\mathbf{+14.40}$ & $+7.39$ & & $+25.81$ & $\mathbf{+31.99}$ & $-0.07$ \\
$20$ & $-7.14$ & $+22.70$ & $+10.19$ & & $+8.32$ & $+17.29$ & $-12.38$ \\
$40$ & $+9.05$ & $+30.62$ & $+4.29$ & & $-4.96$ & $+13.52$ & $+4.41$ \\
\bottomrule
\end{tabular}
\end{table}


% ---------------------------------------------------------------------------
% PROVENANCE OF EVERY VALUE IN THIS TABLE. Each was read from the named script
% at the named pinned commit; values absent from a script come from that repo's
% own argparse defaults. Full machine-readable copy, including the exact argv
% each run was launched with, lives in results/transformer_hyperparameters.md.
% Reproduce any row with:
%   DRY_RUN=1 MODEL=<model> COHORT=<cohort> SEQ_LEN=96 PROFILE=author \
%     [CF_CONFIG=traffic] bash scripts/transformer_bench/anvil_transformer.sb
% which prints the resolved argv and exits without training.
%
% LOOKBACK CAVEAT, stated in the caption because it is load-bearing: PatchTST
% and DeformTime publish ONLY seq_len=336 -- there is no released 96 setting for
% either, so at our protocol length they run at the authors' capacity but not
% the authors' lookback. Crossformer's Traffic script is the single published
% configuration among all three that natively uses in_len=96.
% ---------------------------------------------------------------------------
% \begin{table*}[t]
% \centering
% \caption{Transformer hyperparameters, each at its own authors' published configuration and
% pinned commit. PatchTST and DeformTime publish only \texttt{seq\_len}~$336$, so at our $96$
% they carry the authors' capacity but not their lookback.}
% \label{tab:transformer-hparams}
% \resizebox{\textwidth}{!}{%
% \begin{tabular}{llllll}
% \toprule
% Model & Params & Capacity & Tokenizer & Schedule & Source (pinned commit) \\
% \midrule
% PatchTST & $16{,}836$
%   & \texttt{d\_model 16}, \texttt{n\_heads 4}, \texttt{d\_ff 128},
%     \texttt{e\_layers 3}, \texttt{dropout 0.3}
%   & \texttt{patch\_len 16}, \texttt{stride 8}
%   & \texttt{lr 1e-4}, \texttt{bs 128}, $100$ ep, \texttt{pat.\ 20}, \texttt{type3}
%   & \texttt{yuqinie98/PatchTST@204c21ef} \\
%  & & \texttt{fc\_dropout 0.3}, \texttt{head\_dropout 0} & & & \texttt{scripts/PatchTST/etth1.sh} \\
% \addlinespace
% DeformTime & $67{,}449$
%   & \texttt{d\_model 32}, \texttt{n\_heads 4}, \texttt{e\_layers 2},
%     \texttt{d\_layers 2}, \texttt{dropout 0}
%   & \texttt{n\_reshape 24}, \texttt{patch\_len 6}, \texttt{kernel 7}
%   & \texttt{lr 1e-3}, \texttt{bs 16}, $100$ ep, \texttt{pat.\ 5}, \texttt{type1}
%   & \texttt{ClaudiaShu/DeformTime@cb99f4f6} \\
%  & & & & & \texttt{scripts/DeformTime/ETTh1.sh} \\
% \addlinespace
% \textbf{Crossformer} & $722{,}032$
%   & \texttt{d\_model 64}, \texttt{d\_ff 128}, \texttt{n\_heads 2},
%     \texttt{e\_layers 3}, \texttt{dropout 0.2}
%   & \texttt{seg\_len 12}, \texttt{win\_size 2}
%   & \texttt{lr 1e-3}, \texttt{bs 32}, $20$ ep, \texttt{pat.\ 3}, \texttt{type1}
%   & \texttt{Thinklab-SJTU/Crossformer@c10c8ead} \\
% (\textsc{Traffic}, reported) & & \texttt{baseline 0} & & & \texttt{scripts/Traffic.sh} (\texttt{in\_len 96}) \\
% \addlinespace
% Crossformer & $11{,}185{,}944$
%   & \texttt{d\_model 256}, \texttt{d\_ff 512}, \texttt{n\_heads 4},
%     \texttt{e\_layers 3}, \texttt{dropout 0.2}
%   & \texttt{seg\_len 6}, \texttt{win\_size 2}
%   & \texttt{lr 1e-4}, \texttt{bs 32}, $20$ ep, \texttt{pat.\ 3}, \texttt{type1}
%   & \texttt{Thinklab-SJTU/Crossformer@c10c8ead} \\
% (ETTh1, see text) & & \texttt{baseline 0} & & & \texttt{scripts/ETTh1.sh} (\texttt{in\_len 168}) \\
% \addlinespace
% \textbf{iTransformer} & $817{,}153$
%   & \texttt{d\_model 256}, \texttt{d\_ff 256}, \texttt{n\_heads 8},
%     \texttt{e\_layers 2}, \texttt{d\_layers 1}, \texttt{dropout 0.1}
%   & none (inverted embedding)
%   & \texttt{lr 1e-4}, \texttt{bs 32}, $10$ ep, \texttt{pat.\ 3}, \texttt{type1}
%   & \texttt{thuml/iTransformer@c2426e68} \\
%  & & \texttt{use\_norm 1}, \texttt{activation gelu}, \texttt{embed timeF} & & & \texttt{iTransformer\_ETTh1.sh} (\texttt{seq\_len 96}) \\
% \bottomrule
% \end{tabular}%
% }
% \end{table*}


% ---------------------------------------------------------------------------
% SOURCE: results/hp_grid_orig_ic.csv, results/hp_grid_orig_trade.csv (raw per-cell
% numbers), narrative in results/hp_grid_orig.md. Full factorial
% --number_islands {10,20,40} x --extinction_event_generation_number {250,500,1000},
% 9 cells x 2 cohorts x 10 seeds = 180 runs, all completed. Everything else held at
% the paper's configuration (island_size 10, num_mutations 1, bp_iterations 10,
% max_genomes 10000, --normalize avg_std_dev, no sequence slicing).
%
% ALL NINE CELLS RECOMPUTED from the two CSVs and internally consistent.
%
% THE BASELINE CELL (10, 500) IS THE PAPER'S OWN CAMPAIGN, not a re-run:
% test_output/mse_cohort_202{0,1}_aligned, reproducing Table~\ref{tab:all-datasets}
% exactly (+0.003009 / +0.019496 IC, +14.40 / +31.99 trading).
%
% THE NOISE FLOOR IS THE POINT, and it is measured, not assumed. An independent
% re-run of that SAME configuration (test_output/hp_baseline_cohort_202{0,1}_aligned)
% scored IC +0.0033 / +0.0013 and trading -2.56 / -6.13. Same knobs, same evaluation
% -- verified by reading both submit scripts and scoring both through one pipeline.
% They differ only in which genomes the search happened to find: mpi/examm_mpi.cxx:146
% is MPI_Probe(MPI_ANY_SOURCE, ...), so genome-insertion order is wall-clock dependent
% and --seed fixes the RNG stream, not the sequence of events. The resulting gap --
% 0.0182 IC and 38.1 pp on the 2023 cohort -- EXCEEDS the entire spread of this grid
% (0.0170 IC, 24.3 pp). Every difference in the table fits inside the noise of
% changing nothing.
%
% CAUTION -- results/hp_grid_orig.md contains three figures that do NOT reproduce from
% the CSVs and should not be quoted: it gives the trading spread as 27.5 pp
% (recomputed: 24.3), Pearson IC-vs-trade as -0.12 and Spearman as +0.05 "across the
% 9 cells" (recomputed on 9 cells: +0.19 and +0.28; the -0.14 Pearson it likely meant
% is the 8 swept cells only). The table below uses only recomputed values.
%
% METHODOLOGICAL TRAP, recorded for reuse: cohort_2020_aligned's test split spans TWO
% calendar years (2022-01-04 -> 2023-12-29, 500 rows) unlike cohort_2021_aligned (249
% rows, 2023 only). eval_ensemble_ic.py has no date awareness and averages every row,
% so scoring cohort_2020 unsliced returns a two-year blend: +0.0157 for the baseline
% against a true 2022 figure of +0.0030, a 5x overstatement that looks entirely
% plausible. All cohort_2020 numbers here are sliced to 2022 only.
% ---------------------------------------------------------------------------

% \begin{table*}[t]
% \centering
% \caption{EXAMM island count $\times$ repopulation interval, $9$ cells $\times$ $2$ cohorts $\times$
% $10$ seeds. Algorithm~2 returns at L/S~10, gross annual \%. The bolded $(10,500)$ cell is the
% paper's own campaign; no cell is distinguishable from any other.}
% \label{tab:hp-grid}
% \begin{tabular}{lrrrcrrrcrrr}
% \toprule
% & \multicolumn{3}{c}{Trade year 2022} & & \multicolumn{3}{c}{Trade year 2023} & & \multicolumn{3}{c}{Mean} \\
% \cmidrule(lr){2-4} \cmidrule(lr){6-8} \cmidrule(lr){10-12}
% Islands & $250$ & $500$ & $1000$ & & $250$ & $500$ & $1000$ & & $250$ & $500$ & $1000$ \\
% \midrule
% $10$ & $-2.50$ & $\mathbf{+14.40}$ & $+7.39$ & & $+25.81$ & $\mathbf{+31.99}$ & $-0.07$ & & $+11.65$ & $\mathbf{+23.20}$ & $+3.66$ \\
% $20$ & $-7.14$ & $+22.70$ & $+10.19$ & & $+8.32$ & $+17.29$ & $-12.38$ & & $+0.59$ & $+19.99$ & $-1.10$ \\
% $40$ & $+9.05$ & $+30.62$ & $+4.29$ & & $-4.96$ & $+13.52$ & $+4.41$ & & $+2.05$ & $+22.07$ & $+4.35$ \\
% \midrule
% \textit{Col. mean} & $-0.20$ & $+22.57$ & $+7.29$ & & $+9.72$ & $+20.93$ & $-2.68$ & & $+4.76$ & $+21.75$ & $+2.30$ \\
% \bottomrule
% \end{tabular}
% \end{table*}




\subsection{EXAMM vs Transformers}
\label{sec:examm-vs-transformers}
Transformer models generally perform well on real-world, multidimensional data, yet simpler architectures such as linear models have been claimed to outperform them for time series forecasting~\cite{zeng2023transformers}. We bring these different families of models together to compare their ability to forecast stock returns and how that translates into portfolio returns.

We also compared EXAMM against state-of-the-art time series forecasting models, both Transformer-based and otherwise, in their ability to forecast stock returns and how those predictions translate into portfolio investment returns. We tested the linear model DLinear~\cite{zeng2023transformers}, along with the Transformer-based PatchTST~\cite{nie2022time}, iTransformer~\cite{liu2024itransformer}, DeformTime~\cite{shu2024deformtime}, and Crossformer~\cite{zhang2023crossformer}, with an input length of $96$ and an output length of $1$. We chose an input length of $96$ because it is the most widely reported input sequence length across these models, and an output length of $1$ so that the results are directly comparable to the one-step-ahead forecasts of the EXAMM-evolved RNNs. All other hyperparameters were adopted from the default settings.

% Table~\ref{tab:all-datasets} places three published attention baselines --
% Crossformer, DeformTime and PatchTST -- alongside EXAMM. Each transformer is run
% at its \emph{own} published configuration, taken from the authors' released
% training scripts and executed against the authors' own released implementations,
% rather than under a single protocol imposed by us; the only settings we override
% are those this study forces (input length $96$, horizon $1$, our six input
% fields, and ten seeds with prediction-mean ensembling). Every hyperparameter is
% therefore citable to a specific file at a pinned commit.

% ---------------------------------------------------------------------------
% PARAMETER COUNTS are recomputed by instantiating each model at the EXACT config its
% runs used (read back from the model_id string each run writes, e.g.
% sl96_ll0_pl1_dm16_nh4_el3_d0.3 for PatchTST), not transcribed from the papers.
% Two were wrong before this pass:
%   DLinear     42 -> 194. DLinear is 2*(seq_len*pred_len+1); 42 is the seq_len=20
%               value from the earlier protocol, and every run here is seq_len=96.
%   TimeMixer   77,589 -> 72,997. The first figure came from a probe at d_model=32
%               before the authors' ETTh1 script was read; it specifies d_model=16.
%
% TRAINING TIME is the mean over ~120 portfolio runs per model, all on RTX 4090 at
% nine-way concurrency (see the setup paragraph in Section 4). Dataset A is NOT used
% for this column: iTransformer had no timing there until a dedicated re-run,
% Crossformer-Traffic still has none, and three arms recorded no host or GPU at all.
% EXAMM's figure is wall-clock at 32 MPI ranks from tab:examm-training-cost and is
% CPU time, not GPU -- the two are not comparable and the caption says so.
%
% INFERENCE is single-sample, single-thread CPU (torch.set_num_threads(1) for the
% PyTorch rows; rnn_examples/benchmark_inference.cxx for EXAMM), 300 iterations
% after 20 warmup, all on one laptop. Measured on CPU rather than the training GPU
% on purpose: every model here answers in microseconds on a 4090, so that column
% would show eight equivalent models and erase the size story this table exists to
% tell.
%
% EXAMM's 0.71 us is the mean over five genomes spanning 54-107 weights (range
% 0.46-0.97), timing rnn->forward_pass() alone -- genome load, CSV parsing and
% prediction writing are outside the timed region, matching what model(x) times for
% the PyTorch rows.
%
% TWO CONFOUNDS, both in the footnote because the ratio is doing more work than the
% parameter counts alone. (1) FRAMEWORK: EXAMM is C++, the rest PyTorch, whose
% per-call overhead is ~50-100 us regardless of size -- that is most of DLinear's
% 77 us, since 194 parameters of arithmetic cannot cost that. (2) AMORTISATION:
% EXAMM emits a prediction at every recurrent step, so a 250-step pass yields 250
% predictions, while a transformer consumes a 96-step window per prediction. The
% second is a real architectural property for streaming inference, not an
% accounting artefact; the first is not. Producing 250 predictions costs EXAMM
% 156 us in total against DLinear's 19 ms and DeformTime's 1.4 s.
%
% LATENCY DOES NOT TRACK PARAMETERS, which is the interesting part: iTransformer at
% 817k is 6.5x FASTER than DeformTime at 67k, because depth and sequential structure
% cost more than width. Worth a sentence in prose.
% ---------------------------------------------------------------------------
\begin{table}[t]
\centering
\caption{Model size, training time, and inference latency per prediction. Training is the
mean over ${\sim}120$ portfolio runs per model on an RTX~4090 at nine-way concurrency;
EXAMM's is CPU wall-clock at $32$ MPI ranks and is not comparable to the GPU figures.
Inference is single-sample, single-thread CPU on one machine. EXAMM emits a prediction at
every recurrent step, so one pass over a $250$-step series yields $250$ predictions; the
attention and linear models consume a $96$-step window per prediction.}
\label{tab:param-counts}
\begin{tabular}{lrrr}
\toprule
Model & Parameters & Training & Inference ($\mu$s) \\
\midrule
EXAMM         & $55$--$77$       & $16.2$ min$^{\dagger}$ & $\mathbf{0.71}^{\ddagger}$ \\
DLinear       & $194$            & $9.2$ min  & $77$ \\
GRU-i         & $511$            & $9.5$ min  & $2{,}201$ \\
LSTM-i        & $679$            & $14.7$ min & $2{,}060$ \\
PatchTST      & $16{,}836$       & $19.1$ min & $1{,}210$ \\
DeformTime    & $67{,}449$       & $58.5$ min & $5{,}657$ \\
TimeMixer     & $72{,}997$       & $14.9$ min & $2{,}422$ \\
Crossformer   & $722{,}032$      & $59.9$ min & $5{,}203$ \\
iTransformer  & $817{,}153$      & $8.2$ min  & $870$ \\
\bottomrule
\end{tabular}

\vspace{2pt}
{\footnotesize $^{\dagger}$CPU, $32$ MPI ranks, not comparable to the GPU rows. \quad
$^{\ddagger}$C++; the other rows are PyTorch, which adds ${\sim}50$--$100\,\mu$s of per-call
overhead irrespective of model size. On a Raspberry Pi Zero the same genomes take
$9.59$--$12.04\,\mu$s (Table~\ref{tab:ina219_results}).}
\end{table}

\TODO{Fill DLinear's rows when its GPU re-run lands; the paragraphs above cover
EXAMM, Crossformer, PatchTST and DeformTime only.
%
% Load-bearing points the prose will need to state, all already measured:
%   * PatchTST at its published configuration is the smallest transformer here
%     (16,836 params, ~300x EXAMM's 55-77), and its accuracy is broadly
%     comparable to EXAMM's -- marginally better IC in 2022 (+0.0041 vs
%     +0.0030), worse in 2023 (-0.0089 vs +0.0195), MSE/MAE within a percent
%     both years.
%   * Neither IC difference is statistically resolvable: the paired
%     ensemble-IC difference vs EXAMM is t_HAC=+0.05 (2022) and -1.64 (2023),
%     and EXAMM's own IC only reaches t_HAC=+1.72 at its best. Neither model is
%     distinguishable from zero skill; an argument resting on the IC ordering
%     alone would be reading noise. Say this explicitly rather than letting the
%     table's bolding imply a winner.
%   * Trading is where they separate. At the pre-committed book of ten,
%     PatchTST returns +1.52% (2022) and +12.46% (2023) vs EXAMM's +14.40% and
%     +31.99%; Sharpe 0.18/0.93 vs 0.98/2.32. Net of costs PatchTST's 2022
%     L/S 10 cell turns negative (-1.91%) while EXAMM stays clearly positive
%     (+12.73%).
%   * PatchTST is NOT a failed baseline in the way an under-trained model is --
%     it is mildly profitable at books ten and wider in both years -- but it
%     does not approach EXAMM's risk-adjusted return at any book size while
%     carrying two orders of magnitude more parameters.
}


% ---------------------------------------------------------------------------
% THE FULL GRID (tab:all-datasets). Every panel this paper measures x every model
% x the three metrics on which they actually differ. Laid out like the comparison
% tables in the long-horizon forecasting literature (e.g. Crossformer's Table 1):
% datasets down, models across, best bold and second underlined per row.
%
% WHY NOT MSE/MAE HERE. Across these same fourteen rows the models differ on MSE by
% a median of 2.9% (range 2.1-5.0%). A column pair for it would spend a sixth of the
% table's width demonstrating that point accuracy does not separate anything, which
% is the finding tab:pooling-ablation already makes. MSE/MAE keep their own table.
%
% SHARPE IS GROSS, THE RETURN COLUMN IS NET, and that asymmetry is forced.
% trade_portfolio_daily_curve.py refuses to emit a curve under --use-tc: its
% self-check (instrument the real Portfolio method, rerun the unpatched one,
% require agreement to 1e-6) fails at +12.7248% vs +12.7257%. A net Sharpe would
% need a SECOND implementation of the strategy -- exactly what that script exists
% to prevent. tab:hp-grid-sharpe reports gross for the same reason.
%
% NO DASHES REMAIN: DLinear and DeformTime are now complete on all 14 cells, so
% every entry is measured. The convention is kept for the reason below, which still
% applies to Crossformer (omitted here) and to any future arm:
%   - Crossformer on 8 of the replication cells: the Algorithm 2 gate opens on 0 of
%     ~250 days, so the book never opens. trade_portfolio.py reports +0.00 for
%     these, but "never traded" is not "traded to a flat result" and Sharpe on a
%     flat equity curve is 0/0. Reported undefined, per the note under
%     tab:results-2022-2023. Its cross-sectional dispersion in those cells is
%     4e-6 to 3e-4 -- a near-constant prediction, not a strategy that broke even.
%
% PROVENANCE: every non-EXAMM value is regenerated by a scorer, not transcribed.
% Replication cells come from score_cells.py; Dataset A from score_dataset_a.py,
% which imports score_cells' own ensemble/daily_ic/trade/sharpe so the two panels
% cannot diverge in method. It was validated by reproducing this table's existing
% PatchTST, iTransformer and DeformTime Dataset A entries exactly before DLinear
% was added. EXAMM's values come from the rep3 run trees, which reproduce
% tab:midhighmid's IC on 12 of 12 cells exactly.
%
% DATASET A's 2022 COLUMN IS DATE-FILTERED. cohort_2020's test split spans 501 days;
% scoring it whole returns a two-year blend, which results/hp_grid_orig records as
% +0.0157 against a true +0.0030. IC is restricted by slicing the frame, but RETURNS
% COMPOUND and cannot be -- and a sliced frame fails the trader outright, since
% --align suffix trims to the trailing window while 2022 is the PREFIX. The trader's
% --window-start/--window-end restrict trading dates after alignment instead.
% ---------------------------------------------------------------------------
% CAPTION KEPT SHORT -- the detail it used to carry lives here:
%   * Sharpe is GROSS while the return column is NET. Forced, not chosen:
%     trade_portfolio_daily_curve.py refuses a curve under --use-tc because its
%     self-check fails at +12.7248% vs +12.7257%; a net Sharpe would need a second
%     implementation of the strategy.
%   * DASHES, if any reappear, mark a cell where the Algorithm 2 gate never opened --
%     the book never opens, so +0.00 is not break-even and Sharpe on a flat curve is
%     0/0. Every cell in this table is currently measured.
%   * BOLDING MARKS THE POINT ESTIMATE ONLY. At ~250 trading days the standard error on
%     a Sharpe near 1.0 is ~0.07-0.10, and on IC it is the order of the 0.0084 per-seed
%     spread. Differences below those are not resolvable and no ranking claim rests on them.
%   * CROSSFORMER omitted: at the authors' Traffic configuration it opens the gate on 0 of
%     ~250 days in 8 of the 12 replication cells, so most of its row would be undefined.
%   * EXAMM's parameter count varies by cohort because the architecture is evolved.
\begin{table*}[t]
\centering
\caption{All panels and models. Rank IC, ICIR, return net of transaction costs, and gross
Sharpe ($r_f{=}0$), at the pre-committed L/S~10 book. Bold/underline is best/second per
metric; parameter counts are in the header. Every cell is a 10-run ensemble. LSTM-i and GRU-i are fixed-topology recurrent baselines trained through EXAMM's own pipeline under causal instance normalisation. The three benchmark columns use no predictions at all: buy-and-hold, daily equal-weight rebalancing, and an UNCONDITIONAL $5$-day short-term reversal long/short (Financial\_toolbox @4bab989). Unlike every model column they are not gated by Algorithm~2, so they trade every day. The summary rows average the twelve replication cells and all fourteen; Dataset A is two cells and moves the all-panel mean noticeably for several models.}
\label{tab:all-datasets}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llrrrrrrrrrrrrrrrrrrrrrrrrrrr}
\toprule
Panel & Trade yr & \multicolumn{3}{c}{EXAMM $55$--$77$} & \multicolumn{3}{c}{DLinear $194$} & \multicolumn{3}{c}{PatchTST $16{,}836$} & \multicolumn{3}{c}{iTransf. $817{,}153$} & \multicolumn{3}{c}{DeformT. $67{,}449$} & \multicolumn{3}{c}{TimeMixer $72{,}997$} & \multicolumn{3}{c}{LSTM-i $295$} & \multicolumn{3}{c}{GRU-i $295$} & \multicolumn{3}{c}{Benchmarks, net (\%)} \\
\cmidrule(lr){3-5} \cmidrule(lr){6-8} \cmidrule(lr){9-11} \cmidrule(lr){12-14} \cmidrule(lr){15-17} \cmidrule(lr){18-20} \cmidrule(lr){21-23} \cmidrule(lr){24-26} \cmidrule(lr){27-29}
& & IC & Net (\%) & Sharpe & IC & Net (\%) & Sharpe & IC & Net (\%) & Sharpe & IC & Net (\%) & Sharpe & IC & Net (\%) & Sharpe & IC & Net (\%) & Sharpe & IC & Net (\%) & Sharpe & IC & Net (\%) & Sharpe & B\&H & EW & Rev-5 \\
\midrule
Dataset A & 2022 & +0.0030 & \textbf{+12.73} & \textbf{+0.98} & -0.0019 & -17.75 & -0.87 & +0.0041 & -1.91 & +0.17 & \textbf{+0.0257} & -1.94 & +0.17 & +0.0010 & +4.84 & +0.45 & +0.0065 & +2.93 & +0.43 & \underline{+0.0166} & +1.60 & +0.34 & +0.0143 & \underline{+8.23} & \underline{+0.62} & -14.03 & -22.28 & -24.56 \\
Dataset A & 2023 & \textbf{+0.0195} & \textbf{+30.24} & \textbf{+2.32} & \underline{+0.0050} & \underline{+22.29} & \underline{+1.83} & -0.0089 & +9.18 & +0.93 & -0.0094 & -22.52 & -1.75 & -0.0227 & -24.73 & -1.69 & -0.0013 & -4.91 & -0.09 & -0.0209 & -33.04 & -2.24 & -0.0247 & -31.02 & -2.01 & +7.87 & +0.97 & +20.36 \\
\midrule
set1 & 2022 & -0.0035 & -4.63 & -0.13 & +0.0003 & +6.15 & \textbf{+0.52} & +0.0103 & +3.54 & +0.35 & +0.0159 & +0.80 & +0.25 & +0.0114 & +2.48 & +0.27 & -0.0131 & -0.18 & +0.18 & \textbf{+0.0238} & \textbf{+7.13} & \underline{+0.50} & \underline{+0.0236} & \underline{+6.65} & +0.48 & -7.16 & -14.26 & -25.01 \\
set1 & 2023 & \underline{+0.0112} & \underline{+21.34} & \underline{+1.59} & +0.0069 & -4.27 & -0.06 & +0.0086 & -10.61 & -0.44 & -0.0010 & -0.94 & +0.21 & +0.0025 & -4.64 & -0.11 & \textbf{+0.0158} & \textbf{+23.46} & \textbf{+1.79} & +0.0079 & -5.20 & -0.02 & +0.0064 & +1.48 & +0.35 & +5.18 & -1.67 & +7.78 \\
set1 & 2024 & +0.0082 & \textbf{+17.19} & \underline{+1.11} & \underline{+0.0123} & -15.48 & -0.60 & \textbf{+0.0140} & -23.11 & -1.04 & -0.0025 & -17.28 & -0.84 & +0.0075 & +1.73 & +0.28 & +0.0051 & \underline{+13.39} & \textbf{+1.15} & -0.0090 & -15.49 & -0.61 & -0.0086 & -15.57 & -0.60 & -4.73 & -9.92 & +43.79 \\
\addlinespace
set2 & 2022 & \textbf{+0.0103} & \textbf{+35.22} & \textbf{+1.88} & +0.0037 & +5.66 & +0.55 & +0.0056 & -1.85 & +0.17 & +0.0082 & \underline{+13.72} & \underline{+1.12} & +0.0008 & -1.61 & +0.12 & -0.0040 & -25.23 & -1.51 & \underline{+0.0094} & -4.05 & +0.07 & +0.0091 & +0.45 & +0.29 & -11.42 & -18.91 & -3.46 \\
set2 & 2023 & \underline{+0.0127} & \underline{+16.92} & \underline{+1.26} & +0.0021 & +3.29 & +0.50 & +0.0003 & -10.35 & -0.45 & +0.0003 & -10.34 & -0.43 & -0.0089 & -15.06 & -0.88 & \textbf{+0.0271} & \textbf{+28.56} & \textbf{+2.39} & -0.0056 & -22.25 & -1.14 & -0.0056 & -16.90 & -0.79 & +15.37 & +7.40 & +10.96 \\
set2 & 2024 & \textbf{+0.0181} & \textbf{+13.80} & \textbf{+1.09} & -0.0017 & -12.99 & -0.69 & +0.0001 & -12.85 & -0.68 & -0.0082 & -17.71 & -1.05 & -0.0046 & -14.20 & -0.83 & \underline{+0.0089} & \underline{-2.76} & \underline{+0.17} & -0.0060 & -25.10 & -1.54 & -0.0057 & -25.39 & -1.59 & +12.13 & +3.46 & +31.49 \\
\addlinespace
set3 & 2022 & +0.0122 & \textbf{+36.68} & \textbf{+1.88} & +0.0111 & -1.43 & +0.14 & +0.0099 & +4.27 & +0.45 & \textbf{+0.0304} & +19.65 & +1.19 & +0.0006 & +3.88 & +0.36 & \underline{+0.0167} & \underline{+27.55} & \underline{+1.33} & +0.0120 & +17.38 & +0.86 & +0.0109 & +17.99 & +0.89 & -11.39 & -19.08 & -13.84 \\
set3 & 2023 & -0.0120 & \underline{-3.82} & \underline{-0.22} & -0.0060 & \textbf{+1.03} & \textbf{+0.34} & -0.0061 & -8.87 & -0.30 & \textbf{+0.0052} & -18.84 & -1.11 & -0.0054 & -9.69 & -0.40 & -0.0048 & -13.16 & -0.81 & \underline{+0.0005} & -10.61 & -0.27 & -0.0011 & -13.03 & -0.45 & +14.77 & +7.05 & +7.36 \\
set3 & 2024 & -0.0115 & -2.88 & -0.05 & +0.0003 & \textbf{+24.88} & \textbf{+1.67} & \textbf{+0.0088} & \underline{+11.46} & \underline{+0.86} & +0.0083 & +6.32 & +0.63 & +0.0037 & -4.84 & -0.07 & -0.0010 & +5.62 & +0.73 & \underline{+0.0086} & -1.82 & +0.22 & +0.0070 & -2.98 & +0.15 & +13.11 & +3.89 & -6.14 \\
\addlinespace
set4 & 2022 & \underline{+0.0277} & -5.43 & -0.36 & +0.0027 & +8.14 & +0.72 & +0.0188 & \underline{+20.51} & \underline{+1.30} & +0.0187 & \textbf{+22.58} & \textbf{+1.48} & +0.0039 & -4.32 & -0.05 & -0.0027 & +4.82 & +0.66 & \textbf{+0.0280} & +11.12 & +0.81 & +0.0228 & +11.91 & +0.78 & -10.25 & -17.33 & +2.99 \\
set4 & 2023 & +0.0022 & +1.84 & +0.24 & \textbf{+0.0245} & \textbf{+24.33} & \textbf{+2.17} & \underline{+0.0165} & -7.40 & -0.27 & +0.0161 & \underline{+10.93} & \underline{+1.25} & +0.0021 & -11.13 & -0.54 & -0.0167 & -21.61 & -1.93 & -0.0040 & -20.26 & -1.19 & -0.0018 & -24.45 & -1.46 & +9.41 & +3.16 & +33.45 \\
set4 & 2024 & +0.0065 & +5.96 & +0.58 & +0.0078 & -6.26 & -0.26 & +0.0055 & \textbf{+16.68} & \textbf{+1.50} & -0.0030 & -12.38 & -0.62 & +0.0013 & -9.88 & -0.48 & -0.0030 & \underline{+8.65} & \underline{+0.98} & \underline{+0.0079} & +1.69 & +0.53 & \textbf{+0.0104} & +5.13 & +0.73 & +6.69 & -1.14 & +7.02 \\
\midrule
\textit{Mean, replication} & & +0.0068 & \textbf{+11.02} & \textbf{+0.74} & +0.0053 & +2.75 & +0.42 & \textbf{+0.0077} & -1.55 & +0.12 & \underline{+0.0074} & -0.29 & +0.17 & +0.0012 & -5.61 & -0.19 & +0.0024 & \underline{+4.09} & \underline{+0.43} & +0.0061 & -5.62 & -0.15 & +0.0056 & -4.56 & -0.10 & +2.64 & -4.78 & +8.03 \\
\textit{Mean, all panels} & & \underline{+0.0075} & \textbf{+12.51} & \textbf{+0.87} & +0.0048 & +2.68 & \underline{+0.43} & +0.0063 & -0.81 & +0.18 & \textbf{+0.0075} & -2.00 & +0.04 & -0.0005 & -6.23 & -0.26 & +0.0024 & \underline{+3.37} & +0.39 & +0.0049 & -7.06 & -0.26 & +0.0041 & -5.54 & -0.19 & +1.82 & -5.62 & +6.58 \\
\bottomrule
\end{tabular}%
}\end{table*}




% No trailing \clearpage: there is no \input after this one in sigconf.tex
% yet, so a forced break here only wastes a page. If a new section is added
% after this one, put ONE \clearpage at the START of that new file (matching
% the single boundary-flush pattern used at the top of this file), not here.

\subsection{EXAMM Scalability}

EXAMM is an asynchronous parallel neural architecture search algorithm that scales across different numbers of cores. The table below shows EXAMM's training time as the number of cores varies.

% ---------------------------------------------------------------------------
% TRAINING COST (tab:examm-training-cost). The companion to tab:ina219_results:
% that one measures what the evolved model costs to RUN, this one what it costs
% to FIND. Both are needed -- a 55-parameter model that took a supercomputer to
% discover is a different proposition from one that did not, and a reader who
% only sees inference energy is entitled to ask.
%
% SOURCE: results/scaling/timings.csv, 5 rank counts x 10 repeats = 50 runs, all
% completed. scripts/stock_run/anvil_examm_scaling.sb on Anvil `wholenode`
% --exclusive (a timing study on `shared` measures co-tenants too). Every EXAMM
% flag is byte-identical to anvil_mse_pooled.sb, so the 32-rank row IS the
% paper's own production configuration, not a lighter stand-in; it reproduces
% the 10,031-genome / ~16 min figure in that campaign's own fitness_log.csv.
%
% REPORTED AS TIME, NOT AS SPEEDUP OR EFFICIENCY, and that is deliberate. The
% arms did the same WORK (genomes 10,007-10,127 and BP epochs 98,757-99,965,
% both 1.2% spread) but NOT the same-sized networks: edges_mean spans 13.82-19.19,
% a 39% spread, because EXAMM's genome-insertion order is wall-clock dependent
% (mpi/examm_mpi.cxx MPI_Probe(MPI_ANY_SOURCE)) so rank count changes which
% topologies get explored. That is enough to move an efficiency RATIO by several
% points -- the arm with the largest genomes (16 ranks) looks worst and the one
% with the smallest (128) looks best -- so a "% of linear" column would be
% reporting the confound. Measured wall-clock and measured core-hours are facts
% about what these runs cost; they need no such correction.
%
% THE POINT OF THE TABLE is the pair of columns, not either alone: core-hours are
% flat (8.26-9.88, 20% spread) while wall-clock moves 18.8x. Total compute is
% invariant; how long you wait is a scheduling choice. That is what answers the
% obvious objection to Table~\ref{tab:constructions}'s parameter counts -- EXAMM's
% training cost is parallel cost, and at one node the paper's entire search is
% under four minutes.
% ---------------------------------------------------------------------------
\begin{table}[t]
\centering
\caption{EXAMM training cost for the paper's configuration ($10{,}000$ genomes), by MPI rank
count; $10$ runs each on one exclusive node, $\pm$ is the standard deviation. Total compute
is flat while wall-clock falls $18.8\times$.}
\label{tab:examm-training-cost}
\begin{tabular}{rrrr}
\toprule
Ranks & Workers & Wall-clock (min) & Core-hours \\
\midrule
$8$   & $7$   & $72.8 \pm 15.6$ & $9.70$ \\
$16$  & $15$  & $37.1 \pm 7.7$  & $9.88$ \\
\textbf{32} & \textbf{31} & $\mathbf{16.2 \pm 2.8}$ & $\mathbf{8.65}$ \\
$64$  & $63$  & $8.1 \pm 1.9$   & $8.59$ \\
$128$ & $127$ & $\mathbf{3.9 \pm 1.0}$ & $8.26$ \\
\bottomrule
\end{tabular}
\end{table}




\subsection{Respberry Pi Zero Results}
\label{sec:ina-219}
We evaluate each trained RNN on-device on a Raspberry Pi Zero to characterize inference accuracy, latency, and power consumption under realistic edge-deployment conditions. Each evaluation loads a serialized \texttt{RNN\_Genome} produced by EXAMM~\cite{examm} together with its saved normalization parameters (min--max or mean--standard-deviation), applies the same normalization to the held-out test series, and reports mean squared error (MSE) and mean absolute error (MAE) over the full test set. Wall-clock inference time is measured around the forward pass only (excluding I/O and prediction-writing overhead) using \texttt{std::chrono::high\_resolution\_clock}, from which we derive per-data-point latency and throughput (points/second). Concurrently with inference, an INA219 current/power sensor connected over I2C is polled from a dedicated sampling thread, recording bus voltage, shunt voltage, current, and power at fixed intervals; energy consumption is obtained by trapezoidal integration of power over the sampler's timestamped readings, and normalized by the number of test points to yield an energy-per-inference estimate. This setup allows us to directly relate model size and predictive accuracy to their on-device latency and energy cost.

% CAPTION KEPT SHORT -- detail retained here:
%   * The 32-rank row IS the paper's production configuration and reproduces that
%     campaign's own fitness_log.csv (~16 min for 10,031 genomes).
%   * EXAMM is master-worker, so N ranks give N-1 evaluators.
%   * Reported as TIME, not speedup or efficiency: the arms did the same work (genomes and
%     BP epochs within 1.2%) but not the same-sized networks (edges_mean spans 13.82-19.19,
%     a 39% spread, because insertion order is wall-clock dependent). That moves an
%     efficiency RATIO by several points; measured wall-clock and core-hours need no such
%     correction.
\begin{table*}[t]
\centering
% \footnotesize
\begin{tabular}{lrr}
\toprule
Metric & 2020 & 2021 \\
\midrule
Params              & 55      & 77     \\
Voltage (V)         & 4.72    & 4.71   \\
Current (mA)        & 337.1   & 345.2  \\
Power (mW)          & 1586.6  & 1622.4 \\
Idle Pwr (mW)       & 1131.1  & 1124.5 \\
Gross E/file (mJ)   & 7.61    & 4.86   \\
Net E/file (mJ)     & 2.18    & 1.48   \\
Latency ($\mu$s)    & 9.59    & 12.04  \\
Throughput (k/s)    & 113.3   & 87.7   \\
\bottomrule
\end{tabular}
\caption{Model size, INA219 power/energy, averaged across 10 runs per year on Raspberry PiZero.}
\label{tab:ina219_results}
\end{table*}